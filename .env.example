# Local backup only — Markdup does not load this file.
# The live flow is: paste the token in Markdup Settings → Save token.
#
# Copy this file to `.env`, then paste your classic PAT after the `=`.
# `.env` is gitignored.

MARKDUP_GITHUB_PAT=
