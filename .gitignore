node_modules
dist
.DS_Store
*.local
.vite
*.tsbuildinfo
brand/extension-private.pem

# Local editor / agent config (not shared)
.cursor/

# Local release staging (zips are published via GitHub Releases, not committed)
releases/release-*/

# Local secrets (never commit)
.env
.env.*
!.env.example
